# Open Questions

<!-- Generated by discovery run 2026-06-15T08-21-45.484Z -->

## Q-001

**ID:** Q-001  
**Capability:** infra.service-health  
**Question:** Does `GET /` on the main API return a JSON welcome body or redirect to `/docs`? The deepdive log found a `RedirectResponse` in source, but the capability description says "returns a root JSON welcome/info response."  
**Blocker tier:** nice-to-have  
**Evidence gap:** Source read found `RedirectResponse` at `src/edullm_sat/api/app.py:44` but the exact redirect target was not confirmed.  
**Who can answer:** runtime-observation  
**Status:** open  
**Raised by step:** deepdive

---

## Q-002

**ID:** Q-002  
**Capability:** svg.scene-to-svg  
**Question:** Which LLM provider does the svg-gen component use internally for scene-to-SVG rendering? The surfaces audit labels it "LLM provider" without specifying; the main generation pipeline uses litellm (Gemini + Claude), but svg-gen's dependency was not verified at the source level.  
**Blocker tier:** post-rebuild  
**Evidence gap:** `components/svg-gen/src/svg_gen/` pipeline internals were not read; provider config may be in a separate config file or env var.  
**Who can answer:** source-app-product-owner  
**Status:** open  
**Raised by step:** deepdive

---

## Q-003

**ID:** Q-003  
**Capability:** generation.question-generation  
**Question:** What happens when `SERVICE_AUTH_TOKEN` is set but the caller omits the token — does the API return 401 or 403, and is this documented in the OpenAPI schema?  
**Blocker tier:** before-rebuild  
**Evidence gap:** `verify_auth` dependency was confirmed as a no-op when the env var is unset, but error-path behavior under auth enforcement was not read from source.  
**Who can answer:** runtime-observation  
**Status:** open  
**Raised by step:** coverage-analysis

---

## Q-004

**ID:** Q-004  
**Capability:** question-bank.fetch-and-serve  
**Question:** Is the `question-bank-fetcher` server (port 8765) intended to be run in production alongside the main API, or is it strictly a local development utility like `question-viewer`?  
**Blocker tier:** before-rebuild  
**Evidence gap:** The component is not referenced in the main `Dockerfile` or Cloud Run deployment config based on surfaces audit; confirmation of its operational scope is missing.  
**Who can answer:** source-app-product-owner  
**Status:** open  
**Raised by step:** coverage-analysis

---

## Q-005

**ID:** Q-005  
**Capability:** svg.svg-to-png  
**Question:** What values are accepted for the `X-Output-Width` and `X-Background` request headers on `POST /convert`, and are they validated or passed directly to Playwright?  
**Blocker tier:** nice-to-have  
**Evidence gap:** Headers were noted in the deepdive as present but their validation logic and accepted ranges were not confirmed from source.  
**Who can answer:** agent-rerun  
**Status:** open  
**Raised by step:** deepdive

---

## Q-006

**ID:** Q-006  
**Capability:** image.ai-image-generation  
**Question:** Does the `image-gen` component support any OpenAI model selection (e.g. `dall-e-2` vs `dall-e-3`) via the request body or env var, or is it hard-coded to a single model?  
**Blocker tier:** post-rebuild  
**Evidence gap:** `GenerateJsonRequest` schema has `prompt` and `n` fields confirmed; model selection field was not checked.  
**Who can answer:** agent-rerun  
**Status:** open  
**Raised by step:** coverage-analysis
